# ----------------------------------
# SERVER CONFIGURATION
# ----------------------------------
# We must run this on a different port than auth-service (which is on 8081)
server.port=8082

# ----------------------------------
# DATABASE CONFIGURATION (MySQL)
# ----------------------------------
# This service connects to its OWN database
spring.datasource.url=jdbc:mysql://localhost:3306/ucrmp_claim_db

# IMPORTANT: Replace with your actual MySQL credentials
spring.datasource.username=root
spring.datasource.password=sushant@1234

# ----------------------------------
# FLYWAY CONFIGURATION
# ----------------------------------
# Flyway is enabled by default, this just makes it explicit
spring.flyway.enabled=true

# ----------------------------------
# JPA & HIBERNATE CONFIGURATION
# ----------------------------------
# CRITICAL: We tell Hibernate to ONLY validate the schema.
# Flyway is now in charge of creating and updating tables.
spring.jpa.hibernate.ddl-auto=validate

# This is helpful for debugging
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ----------------------------------
# JWT CONFIGURATION
# ----------------------------------
# This MUST be the same secret key used by the auth-service
app.jwt.secret=bXktc3VwZXItc2VjdXJlLWp3dC1zZWNyZXQta2V5LWZvci11Y3JtcC1wcm9qZWN0LTIwMjU=